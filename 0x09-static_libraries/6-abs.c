#include "main.h"

/**
 * _abs - computes the absolut value of an integer
 * @n: the int vlaue to check
 * Return: Return the absolute value of int
 *
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	return (-n);
}
